include: package:lints/recommended.yaml

analyzer:
  language:
    strict-casts: true
    strict-raw-types: true
    strict-inference: true

linter:
  rules:
    - unawaited_futures
    - prefer_final_locals
    - avoid_dynamic_calls
