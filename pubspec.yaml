name: offline_cache_sync_workspace
publish_to: none

environment:
  sdk: ^3.6.0

workspace:
  - packages/offline_cache_sync
  - packages/offline_cache_sync_test
  - packages/offline_cache_sync_sqlite

dev_dependencies:
  lints: ^5.0.0
